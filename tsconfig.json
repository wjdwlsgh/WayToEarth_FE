{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "typeRoots": [
      "./types", // 우리가 만들 커스텀 타입 폴더
      "./node_modules/@types" // 기본 타입
    ]
  },
  "include": ["src", "types"]
}
